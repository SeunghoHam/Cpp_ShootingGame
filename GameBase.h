#pragma once
#include<d3dx9.h>
class GameBase
{
public:
	static LPD3DXSPRITE _pSprite;
	static FLOAT t;
protected:
};

//구조체 : 데이터(멤버변수)
//클래스 : 데이터(멤버변수) + 기능(멤버함수 == 메서드)

//클래스는 붕어빵 만드는 틀이다. 자료타입?
//객체 - 메모리에 구체화 된 것(인스턴스)

